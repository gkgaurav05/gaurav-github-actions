name: Shell command

on: push

jobs: 
  run-shell-command: 
      runs-on: ubuntu:20.04
      steps:
        - run: |
              echo "Hello world"
              echo "$HOSTNAME"
        
        - run: echo "$GIT_REF" 
