name: Shell command

on: push

jobs: 
  run-shell-command: 
      runs-on: self-hosted
      steps:
        - run: |
              echo "Hello world"
              echo "$HOSTNAME"
              echo "GITHUB_REPOSITORY: ${GITHUB_REPOSITORY}
        
        - run: echo "$GIT_REF" 
